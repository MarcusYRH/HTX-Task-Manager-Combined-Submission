// src/types/skill.ts

/**
 * Represents a skill that can be possessed by developers
 * and required by tasks (e.g., "Frontend", "Backend")
 */
export interface Skill {
  id: number;
  name: string;
}
